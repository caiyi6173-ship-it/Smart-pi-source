# /home/pi/yolo compatibility directory

This directory is no longer the main SmartTCM workspace.

Use /home/pi/SmartTCM as the primary project root.

Kept here:
- venv/ : Python virtual environment
- models/ : compatibility links to active SmartTCM models
- symlinked SmartTCM entry files
- legacy_non_smarttcm/ : archived old YOLO experiments and unrelated scripts
