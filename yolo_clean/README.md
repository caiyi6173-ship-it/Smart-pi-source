# /home/pi/yolo compatibility directory

This directory is no longer the main smartpi workspace.

Use /home/pi/smartpi as the primary project root.

Kept here:
- venv/ : Python virtual environment
- models/ : compatibility links to active smartpi models
- symlinked smartpi entry files
- legacy_non_smartpi/ : archived old YOLO experiments and unrelated scripts
